# Heimwende Advisor API — environment template. Copy to `.env`.
# ALL keys live in this app's env. NEVER expose them to the frontend.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GOOGLE_SOLAR_API_KEY=
CORS_ORIGINS=http://localhost:5173
APP_ENV=dev
